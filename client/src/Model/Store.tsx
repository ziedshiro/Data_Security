export interface StoreAuth {
    storeId?: string,
    districts:object,
    provinces:object,
    subdistricts: object,
    name: string,
    address: string,
    latitude: number,
    longitude: number,
    imgStore: string,
    storeOpen: string,
    storeClose: string,
    rating: number
}
